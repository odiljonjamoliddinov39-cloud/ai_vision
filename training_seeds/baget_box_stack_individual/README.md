# Baget Box Stack Individual Seeds

These are reviewed, versioned production training samples. Every visible
physical carton has its own annotation under the class
`baget_box_stack_individual`.

The deployment imports each unique image once into the persistent YOLO
dataset. Duplicate source photos are intentionally omitted.
